package entityModels;

import java.time.Duration;

import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;

public class Creation extends MyRunner {
	
	public void  entityModeladdButton() throws InterruptedException {
		//Add Button
		
		  test=report.startTest("Click on entity Model addButton");
//		
//	WebDriverWait wait =new webDriverWait(driver,10);
//	webElement element=wait.until(ExpectedConditions.presenceOfElementLocated(null))
}
}